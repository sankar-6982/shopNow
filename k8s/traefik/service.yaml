apiVersion: v1
kind: Service

metadata:
  name: traefik
  namespace: traefik

spec:
  type: LoadBalancer

  selector:
    app: traefik

  ports:
  - port: 80
    targetPort: web
    name: web

  - port: 8080
    targetPort: dashboard
    name: dashboard